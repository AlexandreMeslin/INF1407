"use strict";
{
    const _getWeekDay = (timestamp) => {
        console.log(`${timestamp.nDay}/${timestamp.nMonth}/${timestamp.nYear}`);
    };
    const getWeekDay = (timestamp) => {
        console.log(`${timestamp.nDay}/${timestamp.nMonth}/${timestamp.nYear}`);
    };
    const _getBus = (bus) => {
        console.log(`${bus.line} @ ${bus.uuid}`);
    };
    const getBus = (bus) => {
        console.log(`${bus.line} @ ${bus.uuid}`);
    };
}
